VITE_API_URL=http://localhost:3000

# Vercel Blob — copy from Storage → your store → .env.local tab
# Or run: npx vercel link && npx vercel env pull .env.local
BLOB_STORE_ID=""
BLOB_READ_WRITE_TOKEN=""

# Optional: use your deployed Vercel app's upload route during local dev
# VITE_BLOB_UPLOAD_URL=https://your-app.vercel.app/api/media/upload
